import { createAction } from '@ngrx/store';

export const increase = createAction('[Value Page] increase');
export const decrease = createAction('[Value Page] decrease');
export const reset = createAction('[Value Page] reset');
export const change = createAction('[Value Page] change');
